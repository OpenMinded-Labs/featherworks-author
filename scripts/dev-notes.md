# Dev Notes

- `tauri.conf.json` nutzt lokales Schema, funktioniert erst nach `npm install`.
- Frontend ist aktuell nur statische `index.html`.
- Später: Bundler (Vite) integrieren und `devPath` / `distDir` anpassen.
